name: Deploy to EC2 from ECR

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon Public ECR
        run: |
          aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/l2o9a2s7
      - name: Build and Push Frontend
        run: |
          sudo docker build -t public.ecr.aws/l2o9a2s7/frontend:latest ./frontend
          sudo docker push public.ecr.aws/l2o9a2s7/frontend:latest

      - name: Build and Push API
        run: |
          sudo docker build -t public.ecr.aws/l2o9a2s7/api:latest ./API
          sudo docker push public.ecr.aws/l2o9a2s7/api:latest

      - name: Build and Push DB
        run: |
          sudo docker build -t public.ecr.aws/l2o9a2s7/db:latest ./DB
          sudo docker push public.ecr.aws/l2o9a2s7/db:latest

      - name: Decode EC2 SSH Key
        run: echo "${{ secrets.EC2_KEY }}" | base64 -d > ec2-key.pem

      - name: SSH and Deploy on EC2
        run: |
          chmod 400 ec2-key.pem
          ssh -o StrictHostKeyChecking=no -i ec2-key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_IP }} << 'EOF'
            
            # Stop and remove existing containers
            sudo docker stop frontend || true && sudo docker rm frontend || true
            sudo docker stop api_container || true && sudo docker rm api_container || true
            sudo docker stop db_container || true && sudo docker rm db_container || true

            # Remove the old image
            sudo docker image rm public.ecr.aws/l2o9a2s7/frontend:latest || true
            sudo docker image rm public.ecr.aws/l2o9a2s7/api:latest || true
            sudo docker image rm public.ecr.aws/l2o9a2s7/db:latest || true
            
            # Pull latest images
            sudo docker pull public.ecr.aws/l2o9a2s7/frontend:latest
            sudo docker pull public.ecr.aws/l2o9a2s7/api:latest
            sudo docker pull public.ecr.aws/l2o9a2s7/db:latest

            # Create network if not exists
            sudo docker network inspect banking-network >/dev/null 2>&1 || sudo docker network create banking-network

            # Run DB container
            sudo docker run -d --name db_container --network banking-network -e MYSQL_ROOT_PASSWORD=Neelbera@2330 -p 3307:3306 public.ecr.aws/l2o9a2s7/db:latest
            # Run API container
            sudo docker run -d --name api_container --network banking-network -p 5000:5000 public.ecr.aws/l2o9a2s7/api:latest
            # Run Frontend container
            sudo docker run -d --name frontend --network banking-network -p 80:5173 public.ecr.aws/l2o9a2s7/frontend:latest
          EOF
